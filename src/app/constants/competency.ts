import type { Thresholds } from "@/types/competency";

export const DEFAULT_THRESHOLDS: Thresholds = {
  highMin: 86,
  midMin: 70,
};

export const CATEGORY_LABELS = {
  high: "High (86–100)",
  mid: "Middle (70–85)",
  low: "Low (0–69)",
};
